{{ form_start(form) }}
<div class="row">
    <div class="col-md-12">{{ form_row(form.name, {'attr':{'class':'form-group', 'placeholder': "Saisir une nouvelle liste"}}) }}</div>
</div>
<div class="ml-2"><button class="btn btn-success">{{ button|default('Enregistrer') }}</button></div>
{{ form_end(form) }}




